// FIX #8: AppRoutes is no longer used as the routing layer.
// All routes are declared directly in App.jsx with individual ProtectedRoute wrappers.
// This file is kept for reference only and is no longer imported.
export default function AppRoutes() {
  return null;
}
